name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1

jobs:
  fmt:
    name: Format
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt
      - name: Exclude graph-export (requires private sibling repo)
        run: sed -i '/"crates\/datasynth-graph-export",/d' Cargo.toml
      - run: cargo fmt --all -- --check

  template_drift:
    name: Template Drift Gate (v3.2.1+)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Check no new hardcoded name/description pools + CLAUDE.md crate count
        run: ./scripts/check_template_drift.sh

  clippy:
    name: Clippy
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy
      - uses: Swatinem/rust-cache@v2
      - uses: arduino/setup-protoc@v3
        with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}
      - name: Install system dependencies
        run: sudo apt-get update && sudo apt-get install -y libfontconfig1-dev
      - name: Exclude graph-export (requires private sibling repo)
        run: sed -i '/"crates\/datasynth-graph-export",/d' Cargo.toml
      - run: cargo clippy --all-targets -- -D warnings

  test:
    name: Test (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - uses: arduino/setup-protoc@v3
        with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}
      - name: Install system dependencies (Linux)
        if: runner.os == 'Linux'
        run: sudo apt-get update && sudo apt-get install -y libfontconfig1-dev
      - name: Exclude graph-export (requires private sibling repo)
        run: grep -v '"crates/datasynth-graph-export"' Cargo.toml > Cargo.toml.tmp && mv Cargo.toml.tmp Cargo.toml
        shell: bash
      - run: cargo test --all-targets

  msrv:
    name: MSRV (1.88)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@1.88
      - uses: Swatinem/rust-cache@v2
      - uses: arduino/setup-protoc@v3
        with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}
      - name: Install system dependencies
        run: sudo apt-get update && sudo apt-get install -y libfontconfig1-dev
      - name: Exclude graph-export (requires private sibling repo)
        run: sed -i '/"crates\/datasynth-graph-export",/d' Cargo.toml
      - run: cargo check --workspace

  security:
    name: Security Audit
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - name: Exclude graph-export (requires private sibling repo)
        run: sed -i '/"crates\/datasynth-graph-export",/d' Cargo.toml
      - name: Install cargo-deny
        run: cargo install cargo-deny --locked
      - name: Run cargo-deny
        run: cargo deny check
      - name: Install cargo-audit
        run: cargo install cargo-audit --locked
      - name: Run cargo-audit
        run: cargo audit --ignore RUSTSEC-2024-0437 --ignore RUSTSEC-2024-0429

  coverage:
    name: Code Coverage
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: llvm-tools-preview
      - uses: Swatinem/rust-cache@v2
      - uses: arduino/setup-protoc@v3
        with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}
      - name: Install system dependencies
        run: sudo apt-get update && sudo apt-get install -y libfontconfig1-dev
      - name: Exclude graph-export (requires private sibling repo)
        run: sed -i '/"crates\/datasynth-graph-export",/d' Cargo.toml
      - name: Install cargo-llvm-cov
        run: cargo install cargo-llvm-cov --locked
      - name: Generate coverage
        # v5.0 added new tests in datasynth-group plus heavier integration suites;
        # llvm-cov instrumentation slows them several-fold and the cumulative workspace
        # run now lands around 35-40 min. Bumping to 60 min absorbs that + leaves space
        # for v5.1 tests. If we hit this limit again, switch to per-crate coverage
        # runs in parallel.
        run: cargo llvm-cov --workspace --exclude datasynth-server --lcov --output-path lcov.info
        timeout-minutes: 60
      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v6
        with:
          files: lcov.info
          fail_ci_if_error: false
        env:
          CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}

  benchmarks:
    name: Benchmark Check
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'
    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - uses: arduino/setup-protoc@v3
        with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}
      - name: Install system dependencies
        run: sudo apt-get update && sudo apt-get install -y libfontconfig1-dev
      - name: Exclude graph-export (requires private sibling repo)
        run: sed -i '/"crates\/datasynth-graph-export",/d' Cargo.toml
      - name: Run benchmarks (smoke test)
        run: cargo bench --bench generation_throughput -- --sample-size 10
